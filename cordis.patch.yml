# DSH bundle mount. Install the package through the profile's plugin manager.
# Install with: dsh plugin --profile web add /path/to/dsh-codex-bridge
- insert:
    - id: codex-bridge
      name: 'dsh-codex-bridge'
      config:
        maxParallel: 2
