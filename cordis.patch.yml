# DSH bundle mount. Install the package through the profile's plugin manager.
# For local development use: node scripts/install-local.mjs
- insert:
    - id: codex-bridge
      name: 'dsh-codex-bridge'
      config:
        maxParallel: 2
